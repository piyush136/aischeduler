{
  "name": "ai-task-manager-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix mcp-server && npm install --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix mcp-server\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
